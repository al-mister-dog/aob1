articles:

/articles
    /index (all articles)
    /article
        /[id] (article by article id)
            get, post, update, delete
        /user
            /[id] (all articles by user id)
                
